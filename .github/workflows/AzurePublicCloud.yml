name: azurecloud

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
        with:
          ref: azurecloud
      - name: Create temp dir
        run: |-
          mkdir -p temp
      - name: Install prerequisites
        run: |-
          # https://github.com/actions/virtual-environments/issues/423
          # sudo apt-get update
          sudo apt-get install libxml2-utils
      - name: Download the source list
        run: |-
          curl $(curl "https://www.microsoft.com/en-us/download/confirmation.aspx?id=56519" | xmllint --xpath "string(//*[@class=\"mscom-link failoverLink\"]/@href)" -) -o ./temp/servicetags_public.json
          jq -r '.values[] | select(.name == "Storage") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > AzureStorage.txt
          jq -r '.values[] | select(.name == "Sql") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > AzureSQL.txt
          jq -r '.values[] | select(.name == "EventHub") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > AzureEventHub.txt
          jq -r '.values[] | select(.name == "LogicApps") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > LogicApps.txt
          jq -r '.values[] | select(.name == "AppService") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > AzureAppService.txt
          jq -r '.values[] | select(.name == "AzureCosmosDB") | .properties.addressPrefixes | join("\n")' ./temp/servicetags_public.json > AzureCosmosDB.txt
      - name: Commit
        run: |-
          git config user.name "Automated"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          timestamp=$(date -u)
          git commit -m "Latest data: ${timestamp}" || exit 0
      - name: Push
        run: |-
          git push
